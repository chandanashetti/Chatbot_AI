# Backend Environment Configuration
# Copy this to .env and update with your actual values

# Server Configuration
PORT=5000
NODE_ENV=development

# OpenAI Configuration (OPTIONAL - can be set via admin frontend)
# OPENAI_API_KEY=your_api_key_here_optional_fallback

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/chatbot_ai
MONGODB_DB_NAME=chatbot_ai

# Ollama Configuration (existing)
OLLAMA_BASE_URL=http://localhost:11434

# JWT Secret (for authentication)
JWT_SECRET=your_jwt_secret_key_here_change_in_production
